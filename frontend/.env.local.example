# Copy to frontend/.env.local or set in repo root .env (vite envDir includes parent).
# Clerk Dashboard → API keys → React → Publishable Key
# https://dashboard.clerk.com/~/api-keys
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
